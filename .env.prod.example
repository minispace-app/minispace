# minispace.app Environment Variables — Production

# === Database ===
POSTGRES_USER=garderie
POSTGRES_PASSWORD=YOUR_SECURE_PASSWORD_HERE
POSTGRES_DB=minispace

# === Redis ===
# Default: redis://redis:6379 (internal Docker network)

# === JWT Secrets ===
JWT_SECRET=YOUR_LONG_RANDOM_SECRET_HERE
JWT_REFRESH_SECRET=YOUR_LONG_RANDOM_REFRESH_SECRET_HERE
JWT_EXPIRY_SECONDS=900
JWT_REFRESH_EXPIRY_DAYS=30

# === Super Admin ===
SUPER_ADMIN_KEY=YOUR_SUPER_ADMIN_KEY_HERE

# === Encryption ===
# Master encryption key for file encryption (32 bytes hex, 64 characters)
# Generate with: openssl rand -hex 32
ENCRYPTION_MASTER_KEY=YOUR_64_CHARACTER_HEX_ENCRYPTION_KEY_HERE

# === SMTP Email ===
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@minispace.app

# === App Configuration ===
APP_BASE_URL=https://www.minispace.app
NEXT_PUBLIC_API_URL=/api
NEXT_PUBLIC_WS_URL=/ws

# === Nginx Ports (external to host) ===
NGINX_HTTP_PORT=8762
NGINX_HTTPS_PORT=9506

# === SSL/TLS Certificates ===
# Path to the directory containing fullchain.pem and privkey.pem
SSL_CERTS_DIR=/etc/letsencrypt/live/minispace.app

# === Firebase Cloud Messaging (optional) ===
FCM_API_KEY=

# === Apple Push Notifications (optional) ===
APNS_KEY_PATH=
APNS_KEY_ID=
APNS_TEAM_ID=
APP_BUNDLE_ID=app.minispace.app

# === Logging ===
RUST_LOG=info

# === Docker Images (for docker-compose.prod.yml) ===
GHCR_REPO=gui-bol/garderieconnect
API_VERSION=latest
WEB_VERSION=latest
LANDING_VERSION=latest

# === Backup Encryption (optional) ===
BACKUP_ENCRYPT_PASSWORD=
